# Slim dependency set for the Vercel serverless deployment. Deliberately
# excludes torch/sentence-transformers/chromadb/elasticsearch/crewai —
# they're too large for Vercel's function size limit, and every place that
# uses them in this codebase already lazy-imports with a graceful fallback
# (hashing embedder, in-process BM25, in-memory vector store, direct-LLM
# coding crew). See README's "Runs without any API key" section and
# retrieval/*.py / crew/coding_crew.py for the fallback logic.
fastapi>=0.115,<0.116
pydantic>=2.9,<3.0
python-dotenv>=1.0,<2.0
langgraph>=0.2.34,<0.3
google-genai>=0.7,<0.8
rank_bm25>=0.2.2,<0.3
httpx>=0.27,<0.28
